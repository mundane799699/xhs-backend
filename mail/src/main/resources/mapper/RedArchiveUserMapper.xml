<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mundane.mail.mapper.RedArchiveUserMapper">
    <select id="findByEmail" resultType="com.mundane.mail.entity.RedArchiveUser">
        SELECT * FROM tb_redarchive_user WHERE email = #{email}
    </select>

</mapper>